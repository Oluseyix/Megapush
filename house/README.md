# Local house server (optional legacy)

Prefer the Cloudflare Worker for production.

```bash
cp .env.example .env   # fill locally — never commit
npm install && npm start
```

Only run with a testnet key that holds no mainnet funds.
