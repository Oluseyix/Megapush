{
  "name": "megapush",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "node scripts/build-cf.mjs",
    "cf:build": "node scripts/build-cf.mjs",
    "cf:deploy": "npm run build && npx wrangler pages deploy public --project-name megapush --commit-dirty=true",
    "cf:dev": "npm run build && npx wrangler pages dev public --compatibility-flags=nodejs_compat"
  },
  "dependencies": {
    "viem": "^2.21.54"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^4.14.0"
  }
}
